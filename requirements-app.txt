# Pinned deps for the SQL chat app so it works with the original app.py (no code changes).
# Use: pip install -r requirements-app.txt
# Then add GROQ_API_KEY to .env and run: streamlit run app.py

langchain==0.1.16
streamlit
python-dotenv
langchain-groq
SQLAlchemy
mysql-connector-python
